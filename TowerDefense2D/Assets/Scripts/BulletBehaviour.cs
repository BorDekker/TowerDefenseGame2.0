using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BulletBehaviour : MonoBehaviour
{
    /*
    public float BulletInterval = 2f;

    [SerializeField]
    public GameObject BulletPrefab;

    public int BulletPower;

    public int speed;

    public int LifeTime;

    public void OnCollisionEnter2D(Collision2D other)
    {
        if(CompareTag("Enemy"))
        {
            other.gameObject.GetComponent<EnemyBehaviour>().TakeDamage(30);
            Debug.Log("TakingDamage");
        }

        //if(CompareTag("Enemy"))
        //{
          //  int Hit = DoDamage(GetComponent<EnemyBehaviour>().EnemyHealth, BulletPower);
        //}
    }
    
    public int DoDamage(int EnemyHealth, int Damage)
    {
        return EnemyHealth - Damage;
    }

    // Update is called once per frame
    void Update()
    {
        transform.position += transform.position * speed * Time.deltaTime;

        //int LifeTime -= Time.deltaTime;

        //if (LifeTime == 0)
        {
            Destroy(BulletPrefab);
        }
    }
    */
}
